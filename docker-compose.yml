version: "3"
services:
  mongo:
    image: mongo
    network_mode: "host"
    ports:
      - "27017:27017"
  martian-robots:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env.testing
    volumes:
      - ./.:/usr/src/app/.
